"""All non-secret tunable parameters for the ICT Swing-Options Alert System.

Secrets (TRADIER_TOKEN, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID) come from
environment variables — never set them here.
"""

# ---------------------------------------------------------------------------
# Tickers
# ---------------------------------------------------------------------------
TICKERS_INDEX: list[str] = ["SPX", "XSP", "QQQ"]
TICKERS_SINGLE: list[str] = ["NVDA", "PLTR", "AMD", "TSLA"]

# ---------------------------------------------------------------------------
# Swing-point fractal strength
# ---------------------------------------------------------------------------
SWING_K_LTF: int = 2   # execution timeframes (1H, 15m)
SWING_K_HTF: int = 3   # higher timeframes (4H, Daily)

# ---------------------------------------------------------------------------
# ATR
# ---------------------------------------------------------------------------
ATR_LEN: int = 14

# ---------------------------------------------------------------------------
# Displacement
# ---------------------------------------------------------------------------
DISP_ATR_LEN: int = 10
DISP_MULT: float = 1.5

# ---------------------------------------------------------------------------
# Bias EMAs (daily)
# ---------------------------------------------------------------------------
EMA_FAST: int = 10
EMA_SLOW: int = 20

# ---------------------------------------------------------------------------
# Liquidity
# ---------------------------------------------------------------------------
EQUAL_TOL_ATR: float = 0.1   # equal-high/low tolerance as fraction of ATR
SWEEP_WINDOW: int = 3         # candles to close back through a pool

# ---------------------------------------------------------------------------
# OTE Fibonacci zone
# ---------------------------------------------------------------------------
OTE_LOW: float = 0.62
OTE_HIGH: float = 0.79
OTE_SWEET: float = 0.705

# ---------------------------------------------------------------------------
# Risk / reward
# ---------------------------------------------------------------------------
STOP_BUFFER_ATR: float = 0.1
MIN_RR: float = 1.0

# ---------------------------------------------------------------------------
# Option contract selection
# ---------------------------------------------------------------------------
DTE_MIN: int = 7
DTE_MAX: int = 14
DELTA_MIN: float = 0.45
DELTA_MAX: float = 0.65

# ---------------------------------------------------------------------------
# News blackout window (hours)
# ---------------------------------------------------------------------------
NEWS_BEFORE_H: int = 24
NEWS_AFTER_H: int = 2

# ---------------------------------------------------------------------------
# DXY / SMT
# ---------------------------------------------------------------------------
DXY_MODE: str = "block"   # "block" or "warn"
REQUIRE_SMT: bool = False
